<!-- app/views/artifacts/show.html.erb -->
<div class="max-w-4xl mx-auto py-8">
  <!-- Raw Mermaid Code -->
  <div class="mb-6">
    <h2 class="text-xl font-bold mb -2">Mermaid JS Code</h2>
    <pre class="bg-gray-100 p-4 rounded-lg overflow-x-auto"><code><%= @artifact.model_erd %></code></pre>
  </div>

  <!-- Rendered Diagram -->
  <div class="mb-6">
    <h2 class="text-xl font-bold mb-2">ERD Diagram</h2>
    <div id="mermaid-diagram" class="mermaid bg-white p-4 border rounded-lg">
      <%= @artifact.model_erd %>
    </div>
  </div>

  <!-- Raw Mermaid Code -->
  <div class="mb-6">
    <h2 class="text-xl font-bold mb -2">Mermaid JS Code</h2>
    <pre class="bg-gray-100 p-4 rounded-lg overflow-x-auto"><code><%= @artifact.top_level_roadmap_diagram %></code></pre>
  </div>

  <!-- Rendered Diagram -->
  <div class="mb-6">
    <h2 class="text-xl font-bold mb-2">ERD Diagram</h2>
    <div id="mermaid-diagram" class="mermaid bg-white p-4 border rounded-lg">
      <%= @artifact.top_level_roadmap_diagram %>
    </div>
  </div>

    <!-- Raw Mermaid Code -->
  <div class="mb-6">
    <h2 class="text-xl font-bold mb -2">Mermaid JS Code</h2>
    <pre class="bg-gray-100 p-4 rounded-lg overflow-x-auto"><code><%= @artifact.value_flow_diagram %></code></pre>
  </div>

  <!-- Rendered Diagram -->
  <div class="mb-6">
    <h2 class="text-xl font-bold mb-2">ERD Diagram</h2>
    <div id="mermaid-diagram" class="mermaid bg-white p-4 border rounded-lg">
      <%= @artifact.value_flow_diagram %>
    </div>
  </div>

</div>

