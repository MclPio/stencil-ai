# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  invite_code: MyString1
  admin: one # Use the 'admin' association with the 'one' user fixture
  user: two    # Use the 'user' association with the 'two' user fixture
  expires_at: <%= 1.day.from_now %>
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

two:
  invite_code: MyString2
  admin: two # Use the 'admin' association with the 'two' user fixture
  user: one    # Use the 'user' association with the 'one' user fixture
  expires_at: <%= 1.day.from_now %>
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

paid_user_invite:
  invite_code: "PAIDINVITE123"
  admin: admin_user # Use the 'admin' association
  user: paid_user   # Use the 'user' association
  expires_at: <%= 1.day.from_now %>
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

free_user_invite:
  invite_code: "FREEINVITE456"
  admin: admin_user
  user: free_user
  expires_at: <%= 1.day.from_now %>
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

unused_invite:
  invite_code: "UNUSEDINVITE789"
  admin: admin_user
  # 'user' is intentionally left null for this unused invite
  expires_at: <%= 1.day.from_now %>
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

reusable_invite:
  invite_code: "REUSABLECODE"
  admin: admin_user
  user: nil # This invite is not used by a specific person
  expires_at: <%= 1.year.from_now %>
  reusable: true
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>